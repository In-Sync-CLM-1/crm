# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.*.local

# Supabase migration working files (dumps, audit, verify) — never commit
migration/

# Cloudflare wrangler local state
.wrangler/

# Local agent / Claude config (per-machine)
.claude/

# Archived migration dumps and one-off scripts with hardcoded JWTs
_archive_2026-05-22/

# Ad-hoc scratch / seed / debug scripts (loose tokens often pasted here)
tmp/

# Large local data caches (regenerated on demand)
.voices.json
